module.exports = class {
	constructor (id, startLocation, orientation, initialCommands) {
		this.id = id
		this.startLocation = startLocation
		this.orientation = orientation
		this.initialCommands = initialCommands
	}
}
